<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="All_Messenger.Pages.SettingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:All_Messenger.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="24" MaxWidth="600" HorizontalAlignment="Left">
            <TextBlock Text="Cài đặt" FontSize="28" FontWeight="SemiBold"/>

            <!-- Thông báo -->
            <StackPanel Spacing="12">
                <TextBlock Text="Thông báo" FontSize="16" FontWeight="SemiBold"/>

                <RadioButtons x:Name="NotificationModeGroup"
                              SelectionChanged="NotificationModeGroup_SelectionChanged">
                    <RadioButton x:Name="RadioToast"
                                 Content="Toast – Hiển thị popup thông báo đầy đủ"
                                 Tag="Toast"/>
                    <RadioButton x:Name="RadioSilent"
                                 Content="Im lặng – Chỉ badge số và âm thanh trên taskbar"
                                 Tag="Silent"/>
                </RadioButtons>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>